
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const ChatManagement = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>ჩატის მართვა</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="text-muted-foreground">აქ იქნება ჩატის მართვის ფუნქციონალი.</div>
      </CardContent>
    </Card>
  );
};

export default ChatManagement;
