// Single source of truth for public-facing platform counts.
// Keep CONSERVATIVE and REAL — do not exaggerate (SEO / trust).
// Actual (approx): ~248 verified mechanics, ~548 active services.
export const SITE_STATS = {
  mechanicsLabel: "200+",
  servicesLabel: "500+",
} as const;
